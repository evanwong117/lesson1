import turtle
turtle.Screen().bgcolor("cyan")
turtle.Screen().setup(600,600)
polygon=turtle.Turtle()
for i in range(3):
    polygon.forward(200)
    polygon.right(90)
    polygon.forward(100)
    polygon.right(90)
turtle.done()